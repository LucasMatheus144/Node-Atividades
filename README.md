# 🚀 API Node.js – Auth & Atividades

API desenvolvida em **Node.js + Express + Sequelize**, com autenticação JWT e CRUD de Atividades.

---

## 📦 Requisitos

- Node.js **v18+** (recomendado)
- PostgreSQL
- npm

---

## ⚙️ Instalação

Clone o repositório e, na raiz do projeto, execute:

```bash
npm install
